import { width } from '../../theme';

const itemWith = width / 2 - 30;
export default {
  container: {
    width: itemWith,
    height: itemWith,
    marginTop: 5,
    marginLeft: 20,
    marginRight: 0,
    marginBottom: 0,
    padding: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
  icon: {
    container: {
      marginBottom: 15,
    },
  },
}
