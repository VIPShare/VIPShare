import { colors, size, tip } from '../../theme';

export default {
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  tip: tip.title,
  subTip: tip.subTitle,
}
